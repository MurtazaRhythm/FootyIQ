"""Copy this file to local_config.py and fill in your keys.
local_config.py is gitignored — keys must never be committed.
Deployed environments use the same names as environment variables instead."""

GEMINI_API_KEY = ""
ELEVENLABS_API_KEY = ""
